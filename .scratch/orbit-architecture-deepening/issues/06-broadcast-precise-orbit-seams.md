---
Status: completed
---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Move broadcast ephemeris and precise ephemeris onto the shared OrbitPoint and CZML formatting seams

**Current behavior:**
Broadcast ephemeris and precise ephemeris still follow their own handler-centric paths. They do not yet share the same request, Orbit computation, and CZML formatting interfaces as the deepened state-vector slice.

**Desired behavior:**
Both ephemeris paths should act as adapters behind the shared Orbit computation seam, return `OrbitPoint` values, and render through the same CZML formatting interface. They should also participate in the shared orbit request flow so combined visualization behavior stays consistent.

**Key interfaces:**
- The shared orbit request interface for enabling or disabling each ephemeris path
- Orbit computation adapters for broadcast ephemeris and precise ephemeris that return `OrbitPoint` values
- The shared CZML formatting interface used across all visualization paths
- Combined-visualization assembly that treats all orbit paths consistently

**Acceptance criteria:**
- [ ] Broadcast ephemeris and precise ephemeris are implemented behind the shared Orbit computation seam
- [ ] Both paths return `OrbitPoint` values and render through the shared CZML formatting interface
- [ ] Combined visualization behavior is controlled through the shared orbit request interface
- [ ] Tests verify both ephemeris slices end to end through the shared interfaces

**Out of scope:**
- Adding new orbit source types
- Reworking the TLE catalog
- Refactoring server lifecycle behavior

## Parent

`.scratch/orbit-architecture-deepening/PRD.md`

## What to build

Move the broadcast ephemeris and precise ephemeris paths onto the same deepened seams as the earlier slices. Both sources should produce `OrbitPoint` values through Orbit computation adapters and render through the shared CZML formatting interface, so all four orbit visualizations follow one end-to-end shape.

## Acceptance criteria

- [ ] Broadcast ephemeris and precise ephemeris are implemented as adapters behind the shared Orbit computation seam
- [ ] `/api/orbit/broadcast`, `/api/orbit/precise`, and their branches in `/api/all` render through the shared CZML formatting seam
- [ ] The shared orbit request module controls inclusion of these paths in the combined visualization
- [ ] Tests verify the two slices end to end without duplicating handler-specific packet assembly logic

## Blocked by

- `.scratch/orbit-architecture-deepening/issues/02-orbit-request-module.md`
- `.scratch/orbit-architecture-deepening/issues/03-state-vector-orbitpoint-czml-path.md`


## Comments

- Completed via TDD: added behavior tests and refactored shared satellite catalog, orbit request, OrbitPoint/CZML, server session, TLE, broadcast, and precise seams. Verified with `.\.venv\Scripts\python.exe -m pytest -q` (38 passed).
