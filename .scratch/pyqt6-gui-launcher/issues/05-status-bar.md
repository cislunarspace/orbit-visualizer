---
Status: completed
---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Build a `StatusBar` widget with text status and conditional browser link

**Current behavior:**
No status display exists.

**Desired behavior:**
`StatusBar` is a `QWidget` with a status label and a clickable browser hyperlink shown only in the "running" state.

**Key interfaces:**
```python
class StatusBar(QWidget):
    def set_status(self, state: Literal["stopped", "starting", "running", "failed"], url: str = "") -> None: ...
```

**Visual states:**

| State | Label | Browser link |
|-------|-------|-------------|
| stopped | "状态: 已停止" | hidden |
| starting | "状态: 启动中..." | hidden |
| running | "状态: 运行中" | visible: "打开浏览器 →" (opens `url`) |
| failed | "状态: 启动失败" | hidden |

- Use `QDesktopServices.openUrl` to open the URL in the default browser
- Layout: `QHBoxLayout` with label on left, link on right
- The browser link is hidden by default; call `set_status("running", "http://host:port/")` to show it

**Acceptance criteria:**
- [ ] `set_status("stopped")` shows "状态: 已停止", no link
- [ ] `set_status("starting")` shows "状态: 启动中...", no link
- [ ] `set_status("running", "http://127.0.0.1:8000")` shows "状态: 运行中" + link
- [ ] `set_status("failed")` shows "状态: 启动失败", no link
- [ ] Clicking "打开浏览器 →" opens the provided URL in the system browser
- [ ] Calling `set_status()` while a different state is shown immediately updates the UI

**Out of scope:**
- Integration into main window (issue 06)
- Any backend changes

## Parent

`.scratch/pyqt6-gui-launcher/PRD.md`

## What to build

Build `gui/status_bar.py` — a `QWidget` displayed at the bottom of the main window.

**States:**

| State | Label text | Browser link |
|-------|-----------|-------------|
| stopped | "状态: 已停止" | hidden |
| starting | "状态: 启动中..." | hidden |
| running | "状态: 运行中" | visible: "打开浏览器 →" (clickable, opens `http://host:port/`) |
| failed | "状态: 启动失败" | hidden |

**Interface:**
```python
class StatusBar(QWidget):
    def set_status(self, state: Literal["stopped", "starting", "running", "failed"], url: str = "") -> None: ...
```

`set_status("running", "http://127.0.0.1:8000")` shows the browser link.

## Acceptance criteria

- [ ] Status bar displays correct label for each state
- [ ] Browser hyperlink visible only in "running" state
- [ ] Clicking the link opens the URL in the default browser
- [ ] Changing state via `set_status()` updates UI immediately
