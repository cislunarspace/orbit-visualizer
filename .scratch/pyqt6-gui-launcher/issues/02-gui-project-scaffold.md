---
Status: completed
---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Scaffold the PyQt6 GUI project structure and entry point

**Current behavior:**
The project only has `main.py` as a FastAPI server started via `uvicorn main:app`.

**Desired behavior:**
A `gui/` Python package exists, importable as `python -m gui`, which launches a PyQt6 application window. `pyproject.toml` includes PyQt6. `run.bat` launches the GUI.

**Key interfaces:**
- `gui/__init__.py` — package init (can be empty or `pass`)
- `gui/main.py` — `if __name__ == "__main__": QApplication([]); QMessageBox.about(...)` to verify it runs; replaced in issue 06
- `pyproject.toml` — add `PyQt6` as a dependency under `[project.dependencies]`
- `run.bat` — change command from `uvicorn main:app` to `python -m gui`

**Acceptance criteria:**
- [ ] `gui/` directory is a valid Python package (has `__init__.py`)
- [ ] `python -m gui` starts without import errors and shows a visible window
- [ ] `pyproject.toml` lists PyQt6 in dependencies
- [ ] `run.bat` launches the GUI application
- [ ] Window can be closed immediately without errors

**Out of scope:**
- Full GUI implementation (deferred to issues 04/05/06)
- Any backend changes

## Parent

`.scratch/pyqt6-gui-launcher/PRD.md`

## What to build

Scaffold the PyQt6 GUI project structure:

- Create `gui/` directory with `gui/__init__.py`
- Add `PyQt6` to `pyproject.toml` dependencies
- Create `gui/main.py` as the entry point that creates and shows the main window (stub until issue 06)
- Update `run.bat` to launch `python -m gui` instead of `uvicorn main:app`
- Verify `python -m gui` starts without errors (window can be closed immediately; no full integration needed yet)

## Acceptance criteria

- [ ] `gui/` package exists and is importable
- [ ] `pyproject.toml` includes `PyQt6` as a dependency
- [ ] `run.bat` launches the GUI application
- [ ] App starts and displays a window without crashing
