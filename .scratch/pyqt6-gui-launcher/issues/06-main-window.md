---
Status: ready-for-agent
---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Build the main PyQt6 window composing all widgets and wiring server lifecycle

**Current behavior:**
No main window exists; a stub window is scaffolded in issue 02.

**Desired behavior:**
A single scrollable `QScrollArea` containing all config sections in order, with a Start/Stop button wired to the `ProcessManager` and status reflected in the `StatusBar`.

**Key interfaces:**

`gui/main_window.py`:
```python
class MainWindow(QMainWindow):
    def __init__(self) -> None: ...
```

**Layout (top to bottom):**
1. `QLabel` title: "卫星轨道可视化器"
2. Grouped `QWidget` sections using `QGroupBox`:
   - "轨道类型" — orbit type checkboxes from `orbit_config`
   - "轨道参数" — hours/step/base_time/r0 fields from `orbit_config`
   - "TLE 输入" — TLE preset + line1/line2 from `tle_input`
   - "服务器配置" — host + port from `server_config`
   - "控制" — Start/Stop `QPushButton`
3. `StatusBar` at the bottom (outside scroll area)

**Behavior:**
- **Start button** (`QPushButton`):
  - Reads `server_config.get_config()` → `host`, `port`
  - Calls `process_manager.start(host, port)`
  - Calls `status_bar.set_status("starting")`
  - Button label changes to "停止服务器", disabled state while transitioning
- **`process_manager.started.connect`** → `status_bar.set_status("running", f"http://{host}:{port}/")`
- **`process_manager.failed.connect`** → `QMessageBox.critical(self, "服务器错误", error_msg)`, then `status_bar.set_status("failed")`
- **Stop button** → `process_manager.stop()`, `status_bar.set_status("stopped")`
- **`process_manager.stopped.connect`** → `status_bar.set_status("stopped")`
- **Start/Stop button label**: `"启动服务器"` when stopped, `"停止服务器"` when running; toggles on state change
- **Close event**: if `process_manager.is_running()`, call `process_manager.stop()` before closing

**Acceptance criteria:**
- [ ] Window opens showing all 5 config sections in scrollable area + status bar at bottom
- [ ] Clicking "启动服务器" starts the server and shows "状态: 启动中..."
- [ ] Server ready → status shows "状态: 运行中" + browser link
- [ ] Server crash → error dialog appears + status shows "状态: 启动失败"
- [ ] Clicking "停止服务器" stops the server and resets status to "已停止"
- [ ] Button label correctly reflects current server state
- [ ] Closing the window while server is running cleanly stops the server first

**Out of scope:**
- Backend modifications (issue 01)
- Process manager implementation (issue 03)
- Individual widget implementation (issues 04/05)

## Parent

`.scratch/pyqt6-gui-launcher/PRD.md`

## What to build

Build `gui/main_window.py` — the main application window that composes all widgets and wires everything together.

**Layout (top to bottom, single scrollable column):**
1. Title: "卫星轨道可视化器"
2. Section: "轨道类型" — orbit type checkboxes (from `orbit_config`)
3. Section: "轨道参数" — orbit params (hours, step, base_time, r0)
4. Section: "TLE 输入" — TLE preset + line1/line2
5. Section: "服务器配置" — host + port
6. Section: "控制" — Start / Stop `QPushButton` (toggles label: "启动服务器" / "停止服务器")
7. Status bar (from `status_bar`)

**Behavior:**
- Start button: reads all config, calls `process_manager.start(host, port)`, sets status to "starting"
- Process manager `started` signal: sets status to "running", shows browser link
- Process manager `failed` signal: shows `QMessageBox.critical` error dialog, sets status to "failed"
- Stop button: calls `process_manager.stop()`, sets status to "stopped"
- Process manager `stopped` signal: sets status to "stopped"

## Acceptance criteria

- [ ] Window opens with all sections visible in correct order
- [ ] Start button spawns server and shows "启动中" status
- [ ] Server ready → "运行中" + browser link appears
- [ ] Server error → error dialog shown + "启动失败" status
- [ ] Stop button terminates server and resets to "已停止"
- [ ] Start/Stop button label toggles correctly
- [ ] Closing window while server running stops the server cleanly
