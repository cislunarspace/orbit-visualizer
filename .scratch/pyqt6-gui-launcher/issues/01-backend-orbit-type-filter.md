---
Status: ready-for-agent
---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Add `?types=` filter parameter to `GET /api/all` endpoint

**Current behavior:**
`GET /api/all` always returns all four orbit types (state_vector, tle, broadcast, precise) as CZML packets, regardless of client needs.

**Desired behavior:**
`GET /api/all?types=state_vector,tle` returns a CZML document containing only the `document` packet plus the requested orbit types. Invalid type names are silently ignored. If `types` is absent or empty, all four types are returned (backwards-compatible).

**Key interfaces:**
- `main.py` — `GET /api/all` handler currently calls all four orbit generators unconditionally
- `make_czml(name, data, color)` — builds a two-packet CZML list (document + orbit). Returns `list[dict]`.
- `generate_state_vector_orbit`, `tle_to_orbit`, `generate_broadcast_ephemeris`, `generate_precise_ephemeris` — orbit generation functions, unchanged
- The CZML document format uses Cesium-compatible JSON with `id`, `version`, `clock`, and per-orbit `position`/`path`/`point` packets

**Acceptance criteria:**
- [ ] `GET /api/all?types=state_vector` returns only state_vector orbit packet + document packet
- [ ] `GET /api/all?types=state_vector,tle` returns exactly those two orbit packets + document packet
- [ ] `GET /api/all` (no param) returns all four orbit packets + document packet — identical to current output
- [ ] Invalid type names (e.g. `?types=invalid`) are silently ignored; valid types are returned
- [ ] CZML `document` packet is always the first element in the returned list

**Out of scope:**
- Changes to individual orbit generation functions
- Changes to other API endpoints
- Adding authentication or rate limiting

## Parent

`.scratch/pyqt6-gui-launcher/PRD.md`

## What to build

Modify `GET /api/all` in `main.py` to accept a `?types=` query parameter. The value is a comma-separated list of orbit type names: `state_vector`, `tle`, `broadcast`, `precise`. The endpoint returns a CZML document containing only the requested orbit types (plus the required `document` CZML packet). If `types` is absent or empty, all four orbit types are returned (backwards-compatible).

No changes to the individual orbit generation functions (`generate_state_vector_orbit`, `tle_to_orbit`, etc.) are needed.

## Acceptance criteria

- [ ] `GET /api/all?types=state_vector` returns only the state vector orbit
- [ ] `GET /api/all?types=state_vector,tle` returns exactly those two
- [ ] `GET /api/all` (no query param) returns all four — identical to current behavior
- [ ] Invalid type names are silently ignored (only valid types are returned)
- [ ] CZML `document` packet is always included as the first element
