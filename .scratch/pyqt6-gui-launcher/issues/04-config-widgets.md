---
Status: completed
---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Build three PyQt6 configuration widget modules for orbit types, TLE input, and server config

**Current behavior:**
No GUI widgets exist.

**Desired behavior:**
Three standalone widget modules, each returning a `QWidget` and exposing a `get_config()` method (or `get_tle()` for TLE).

### `gui/orbit_config.py`

- 4 `QCheckBox` for orbit types with labels "状态向量 (state_vector)", "TLE (tle)", "广播星历 (broadcast)", "精密星历 (precise)" — all checked by default
- `QSpinBox` labeled "投影时长" for hours (range 1–168, default 24)
- `QSpinBox` labeled "时间步长 (秒)" for step (range 1–3600, default 60)
- `QDateTimeEdit` labeled "起始时间" for base_time (default: current UTC datetime)
- `QDoubleSpinBox` labeled "轨道半径 (m)" for r0 (range 6e6–10e7, default 7e6)
- `get_config() -> dict` returns: `{types: list[str], hours: int, step: int, base_time: datetime, r0: float}`
  - `types` is a list of the `value()` attribute of each checkbox (e.g. `["state_vector", "tle"]`)

### `gui/tle_input.py`

- `QComboBox` preset dropdown with items: "ISS", "Hubble", "Starlink-1452", "Custom" — default "ISS"
- `QLineEdit` labeled "TLE Line 1"
- `QLineEdit` labeled "TLE Line 2"
- Preset TLE data:

  | Preset | Line 1 | Line 2 |
  |--------|--------|--------|
  | ISS | `1 25544U 98067A   25123.56789017  .00000012  00000-0  12345-6 0  1234` | `2 25544  51.6400  10.0000 0001234  10.0000  350.0000 15.48999999123456` |
  | Hubble | `1 20580U 90039B   25123.50000000  .00000000  00000-0  00000-0 0  9999` | `2 20580  28.4700 325.0000 0001234  10.0000 200.0000 15.48999999123456` |
  | Starlink-1452 | `1 44083U 19029Q   25123.50000000  .00000000  00000-0  00000-0 0  9999` | `2 44083  53.0000  10.0000 0001234  10.0000 350.0000 15.50000000123456` |

- Selecting a preset populates both line fields; switching to "Custom" clears them
- `get_tle() -> tuple[str, str]` returns (line1, line2)

### `gui/server_config.py`

- `QLineEdit` for host (default `"127.0.0.1"`)
- `QSpinBox` for port (range 1024–65535, default 8000)
- `get_config() -> dict` returns: `{host: str, port: int}`

**Acceptance criteria:**
- [ ] All four orbit type checkboxes exist and are checked by default
- [ ] Hours, step, base_time, r0 fields exist with correct ranges and defaults
- [ ] `orbit_config.get_config()["types"]` lists only checked orbit types
- [ ] TLE preset dropdown has exactly ISS, Hubble, Starlink-1452, Custom
- [ ] Selecting a preset populates line1 and line2; "Custom" clears them
- [ ] Server host defaults to "127.0.0.1", port to 8000

**Out of scope:**
- Main window composition (issue 06)
- Process management (issue 03)

## Parent

`.scratch/pyqt6-gui-launcher/PRD.md`

## What to build

Build `gui/orbit_config.py`, `gui/tle_input.py`, and `gui/server_config.py` as three standalone widget modules. All expose a `widget` property returning the `QWidget` and a `get_config()` method returning a typed dict.

### `gui/orbit_config.py`

- 4 `QCheckBox` for orbit types: "状态向量 (state_vector)", "TLE (tle)", "广播星历 (broadcast)", "精密星历 (precise)" — all checked by default
- `QSpinBox` for hours (label: "投影时长", range 1–168, default 24)
- `QSpinBox` for step in seconds (label: "时间步长", range 1–3600, default 60)
- `QDateTimeEdit` for base_time (label: "起始时间", default: current UTC now)
- `QDoubleSpinBox` for r0 in meters (label: "轨道半径", range 6e6–10e7, default 7e6)
- `get_config() -> dict` returns: `{types: list[str], hours: int, step: int, base_time: datetime, r0: float}`

### `gui/tle_input.py`

- `QComboBox` preset dropdown: "ISS", "Hubble", "Starlink-1452", "Custom" — default "ISS"
- `QLineEdit` for line1 and line2 (label: "TLE Line 1" / "TLE Line 2")
- Selecting a preset populates both line fields with the preset TLE
- Switching to "Custom" clears both fields
- `get_tle() -> tuple[str, str]` returns (line1, line2)

### `gui/server_config.py`

- `QLineEdit` for host (default "127.0.0.1")
- `QSpinBox` for port (range 1024–65535, default 8000)
- Basic validation: red border on invalid port
- `get_config() -> dict` returns: `{host: str, port: int}`

## Acceptance criteria

- [ ] All four orbit type checkboxes present and checked by default
- [ ] Orbit param fields (hours, step, base_time, r0) present with correct defaults
- [ ] `get_config()` returns correct dict structure for orbit config
- [ ] TLE preset dropdown has ISS, Hubble, Starlink-1452, Custom
- [ ] Selecting a preset fills line1 and line2; switching to Custom clears them
- [ ] `get_tle()` returns correct tuple
- [ ] Server host defaults to 127.0.0.1, port defaults to 8000
- [ ] `get_config()` returns correct dict structure for server config
